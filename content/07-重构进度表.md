# 沧海巨浪重构进度表(CANONICAL v1.2 施工追踪)

**依据**:`docs/00-CANONICAL-ARCHITECTURE-v1.md` §7 施工路线图(T0-T5)
**最后更新**:2026-07-04 21:00
**当前阶段**:T1 进行中

**更新规则**:每次施工完成一个动作就勾一次,不要等一个 T 阶段全部完成再更新,防止进度表本身过期漂移(重蹈 CANONICAL §0.2 的教训)。

---

## T0 · 契约冻结 & 骨架落地 —— ✅ 基本完成

| 动作 | 状态 | 备注 |
|---|:-:|---|
| 用户 sign-off CANONICAL v1.2 | ✅ | 2026-07-04 深夜 |
| 目录树骨架(data/raw,research,plans,execution,reports,corrections,evolution,human_thesis) | ✅ | 已建 |
| `docs/agents/` 补 P1/P2/R7 三份 spec | ✅ | R7 ✅(既有);P1/P2 ✅ 2026-07-04 19:00 新补,`docs/agents/P1-竞价校准Agent.md` / `P2-午盘修订Agent.md`,均为 skeleton 状态,具体触发阈值留 T3 施工时按 observe-only 结果确定 |
| `docs/amendment_schema.md` 起草 | ✅ | 2026-07-04 19:00,契约 2 skeleton v1,覆盖 auction(P1)/midday(P2) 两种场景通用结构,字段表+枚举+示例 JSON+版本历史,风格对齐 `watchlist_plan_schema.md`/`research_summary_schema.md` |
| `docs/research_summary_schema.md` 起草 | ✅ | 契约 3 首版 skeleton,见 `docs/research_summary_schema.md` |
| `data/user_preferences.yaml` 首版落盘 + sign-off | ✅ | 龙头偏好、硬风控阈值、板块名单等已落盘 |
| `data/source_diversity_matrix.md` 首版落盘 | ✅ | 待逐号标注意识形态倾向(见文件内待办) |
| `git init` 仓库化 | ✅ | 2026-07-04 18:40,commit `42ac1ad`。**不是"重构前纯净快照"**(重构已到 T1 中期),用户先决定推迟、后改主意当场执行;首次 commit 前做了明文密钥排查,详见下方"git init 密钥排查记录" |
| 现存 cron 保留运行不动 | ✅ | 未触碰旧 crontab / hermes cron |
| Hermes 侧(`~/.hermes/scripts/`)业务逻辑清零 | ✅ | 发现 `xtick_mcp_server.py`(763行真实MCP server)、`qmt_trade_alerts.py`(废弃stub)长期只存在于 `~/.hermes/` 未入项目仓库;已迁移/归档,规则见 `docs/hermes-project-boundary.md` |
| 虚拟环境收敛为一套 | ✅ | 2026-07-04 分两步:①项目根 `.venv-mcp`(929M,Python 3.11.15)确认零引用孤儿,已删除;②当晚二次施工,用户要求彻底消灭 `~/.hermes/xtick-mcp-venv` 例外——把 `.venv` 从 3.9.6 原地升级到 Python 3.11.15(`~/.local/bin/python3.11`)并原生装 `mcp>=1.2.0`,分层验证(基础 import、pytest 98/115 通过且 17 个失败与 3.9 环境逐条一致故非回归、3 条 T1 pipeline 脚本、sise/charts MCP server 确认走 `_USE_FASTMCP=True`、`xtick_mcp_server.py`、`run_weflow_snapshot.py` 真实联调均通过)后正式切换,`~/.hermes/xtick-mcp-venv`(52M)与 3.9 备份 venv(681M)均已删除。**现状:项目仅一套 `.venv`(3.11.15,原生 `mcp`),零系统级例外 venv**;详见 `docs/hermes-project-boundary.md` §虚拟环境收敛 |

**T0 遗留阻塞项**:已于 2026-07-04 19:00 全部补齐(见上表),T0 无遗留阻塞项。

**git init 密钥排查记录(2026-07-04 18:40,commit `42ac1ad` 之前)**:

| 文件 | 问题 | 处理 |
|---|---|---|
| `.cursor/mcp.json` | 明文 `WECHAT_MP_AUTH_KEY`/`TUSHARE_TOKEN`/`XTICK_TOKEN` | 加入 `.gitignore` |
| `config/hermes_mcp.json` | 同上 + `X-Agent-Plan-Key` | 加入 `.gitignore` |
| `chzl_kg/.obsidian/plugins/obsidian-local-rest-api/data.json` | 明文 `apiKey` | 整个 `chzl_kg/.obsidian/`(Obsidian 本地应用状态,非笔记内容)加入 `.gitignore` |
| `testtushare.py`(根目录调试脚本) | 硬编码真实 Tushare token | 加入 `.gitignore`(未删除,仍在本地可用) |
| `chzl_kg/投研交易/MCP数据源全景.md` | 正文明文写了 Tushare token + ARK API key | 脱敏为"见 `.env` 对应变量名",不改变文档结构 |
| `.venv-mcp/`(929M,独立 venv) | 差点被 22565 个文件一起提交进去 | 加入 `.gitignore`(`.venv-*/` 通配) |

排查方法:对全部 816 个待提交文件做了正则扫描(`[a-f0-9]{28,}`、`token=`、`apikey` 等模式),逐一人工确认真假阳性
(如 `run_id`/hash 类字段是假阳性,予以保留)。`os.environ.get(...)` 读取环境变量的源码本身不是泄漏,不受影响。

---

## T1 · Profile 新建 + 数据流验证 —— 🔶 进行中(约 85%,R1-R7 全部真实产出,主要剩余项是 D1 六步决策算法)

| 动作 | 状态 | 备注 |
|---|:-:|---|
| 5 个新 profile 创建(`canghai-{fetch,research,plan,execute,review}`) | ✅ | `hermes profile list` 确认存在,均为 stopped(未启动 daemon) |
| 每个 profile 的 `SOUL.md` | ✅ | 5 份已写,含 Purpose/Ownership/Toolsets/Consumers/Constraints |
| 5 profile 统一指向 `skills/` 目录(`config.yaml` external_dirs) | ✅ | 单一 skill 副本,不再各 profile 本地维护 |
| 16 个旧 skill 按 §4.3 迁移映射 | ✅ | 4 个归档到 `skills/_archive/`(funds_validator/pattern_recognizer/risk_manager/holding_optimizer),3 个改名,内容已合并 |
| 15 个新 skill 骨架创建 | ✅ | `SKILL.md` 骨架均已建,**内容都是 skeleton,未写实际 prompt/logic** |
| **fetch 先跑通**:07:15 抓数据写 `data/raw/YYYYMMDD/` | ✅ 首跑通 | 2026-07-04:100 篇公众号 + 51 条群消息 + MCP 健康;见 `data/raw/20260704/` |
| Manifest / MCP 健康检查上线 | ✅ | `mcp_health.py` + `manifest.json` + `data/raw/manifest.json` 最新指针 |
| research 主控 pull fetch 产物,delegation 调 R1-R7(可先用旧 skill 内容跑通链路) | ✅ R1-R7 全部真实产出 | 2026-07-04:`src/research/orchestrator.py` + CLI;20260704 落盘 7×R*.json + research_summary.json;**R1-R7 均已升级为规则版 v1 真实产出**(R1/R2 见"canghai-research R1/R2 子进度",R3/R4 见"canghai-research R3/R4 子进度",R5/R6/R7 见下方"canghai-research R5/R6/R7 子进度");`orchestrator_status` 现已能跑到 `complete`(20260704 实测);delegation 用确定性 Python 规则替代 LLM sub-agent(cron 无 LLM 会话,设计理由见 `src/research/orchestrator.py` 顶部 docstring) |
| D1 pull research summary 出 plan,与旧 plan 并行对比 | ✅ skeleton 首跑通(未跟进真实算法) | 2026-07-04:`src/plan/main_decision.py` + CLI;校验 research_summary 契约完整性;D1 六步决策算法(§3)尚未实现,`execution_pool`/`watch_pool` 恒为空池;**2026-07-04 晚间更新**:R5/R6/R7 升级为真实产出后 `orchestrator_status` 首次变为 `complete`,`is_skeleton`(`orchestrator_status in ("skeleton","partial")`)随之判 `False`,`mode` 字段从 `dry_run` 翻转为 `live`(此前 R1/R2 子进度就已预判到这个"地雷",本次真实触发)——**但功能上仍然安全**:`autonomous_trading_enabled=False`/`requires_user_confirmation=True`/`execution_pool=[]`/`watch_pool=[]` 均未受影响,只是 `mode` 标签本身在 D1 六步算法接入前具有误导性(标"live"但没有任何真实选股发生),本次未修改 `main_decision.py`(遵照任务要求),留待用户决策见"已知风险"表新增第4项;落盘 `data/plans/20260704/watchlist_plan.json` + `legacy_comparison.json`;**尚无真实选股算法** |
| 旧 profile 继续供 execute 消费(不切换) | ✅ | 未动旧系统,`data/runtime` 已清理但保留最近 10 天供旧 pipeline 用 |

**canghai-fetch 子进度**:

| 子任务 | 状态 | 备注 |
|---|:-:|---|
| MCP 健康检查 `src/fetch/mcp_health.py` | ✅ | |
| AuthKey 自动同步 `scripts/sync_wechat_mp_auth.py` | ✅ | 3388 → `.env` → register |
| 微信公众号 + wechat-cli 5 群 | ✅ | `src/fetch/wechat_sources.py` |
| `fetch_all_sources.py` 主编排 + 落盘 | ✅ | 首跑 20260704 |
| Cron 入口 `scripts/fetch/run_fetch_all_sources.sh` | ✅ | Hermes `e7992d23d1d9` 07:15 Mon-Fri |
| MCP 健康巡检 Hermes cron | ✅ | `5a5e53096dbb` */30 |
| macOS crontab + 旧 Hermes cron | ✅ 已清空 | 2026-07-04 删除 6 个旧 Hermes job + 全部 crontab 业务行 |
| WeFlow snapshot(可选) | ✅ | 2026-07-04 已实现直连方案:`scripts/fetch/run_weflow_snapshot.py` 是自包含脚本(只依赖 `mcp` SDK + 标准库,不 import 其他项目模块),用 `mcp.client.sse` 直连 `weflow-analytics` MCP 调 `export_analytics_snapshot`,**全程无需 agent/LLM session**。**2026-07-04 晚间更新**:`.venv` 已升级到 3.11.15 原生装 `mcp`(见上方"虚拟环境收敛"),`fetch_all_sources.py` 现在用主 `.venv` 直接 `subprocess` 调用本脚本,不再依赖任何独立 venv;45s 超时,任何失败降级为 `status:error/skipped` 不阻塞整体 fetch、不计入 severity。已跑通:`data/raw/20260704/weflow_snapshot.json` + `data/cache/weflow_snapshot_latest.json`,80 个热词。**排坑记录**:`sse_client`/`ClientSession` 内部用 anyio TaskGroup,外面套 `asyncio.wait_for` 会导致 cancel scope 跨 task 边界被打断、抛出内容为空的 `ExceptionGroup`,改用 `anyio.fail_after` 解决。不需要新建 cron,已随现有 07:15 fetch cron 一起跑 |

**canghai-research R1/R2 子进度**(2026-07-04 19:10 新增):

| 子任务 | 状态 | 备注 |
|---|:-:|---|
| R1 风格判断:规则版 v1(非 LLM) | ✅ | `src/research/r1_style.py`;5 维输出(风格/情绪浓度/主线数量/仓位上限/一句话结论)全部落地;数据源:Tushare 直连(`limit_list_d`/`limit_step`/`index_daily`/`limit_cpt_list`)+ `wechat_cli_5groups.jsonl` KOL 观点摘录;confidence 0.55(20260704 实跑值),0.40-0.60 区间随是否精确命中风格档位浮动 |
| R2 主线板块:规则版 v1(非 LLM) | ✅ | `src/research/r2_main_sectors.py`;板块打分只用可拿到真实数据的"热度+资金+涨停"3 维(等比例放大到 100 分口径,叙事/共振因 R3/R4 仍 skeleton 而留空);龙头筛选用连板数+成交额代理 + 市值≥50亿过滤,`dc_concept.lead_stock` 兜底;数据源:`limit_cpt_list`/`ths_hot`/`moneyflow_ind_dc`/`dc_concept`/`dc_concept_cons`/`limit_list_d`;confidence 0.45(有核心主线)/0.30(无) |
| `src/research/data_sources.py`(Tushare 直连封装) | ✅ | 新增 `TushareResearchClient` + 板块名模糊匹配(`fuzzy_match_board`),解决跨数据源命名不一致(如"新能源汽车" vs "新能源车") |
| `run_research_orchestrator.py --date 20260704` 重跑验证 | ✅ | `orchestrator_status: partial`(R1/R2 complete,R3-R7 skeleton);`confidence: {R1: 0.55, R2: 0.45, R3-R7: 0}` |
| D1 侧防误判加固 | ✅ | `src/plan/main_decision.py` 的 `is_skeleton` 判断扩展为 `orchestrator_status in ("skeleton", "partial")`,避免 D1 六步算法未实现时因为 R1/R2 有数据就误出 `mode="live"` 空转 plan |
| R3-R7 真实产出 | ✅ 全部完成 | R3/R4 见"canghai-research R3/R4 子进度",R5/R6/R7 见"canghai-research R5/R6/R7 子进度";R2 的叙事/共振维度回填仍是刻意保留的范围边界(未做,见下方说明) |

**canghai-research R3/R4 子进度**(2026-07-04 20:00 新增):

| 子任务 | 状态 | 备注 |
|---|:-:|---|
| R3 情绪与热度:规则版 v1(非 LLM) | ✅ | `src/research/r3_sentiment.py`;三层数据源交叉(Tushare `ths_hot`/`moneyflow_ind_dc` 热榜 + `wechat_cli_5groups.jsonl` 5 群 KOL 文本子串匹配 + `weflow_snapshot.json` 80 热词)算共振主题(top5)+ 情绪浓度(R1基准+共振加成)+ KOL明日方向摘要 + 热榜信号透传(rank_momentum_up/new_in_top/distribution_alert)+ 数据源健康度五维输出;XTick 因无 MCP 会话完全跳过(标注 `not_available`);confidence 0.55(20260704 实跑值,三源健全基准),按 Tushare/wechat-cli/WeFlow/R1 各自缺失分别扣 0.25/0.15/0.05/0.05 |
| R4 消息面:规则版 v1(非 LLM) | ✅ | `src/research/r4_news.py`;聚合 `wechat_official_20.jsonl`(20号公众号)+ `wechat_cli_5groups.jsonl` 中"盘中新闻"/"专注核心"两群,用 Tushare `dc_concept` 板块名全集做子串实体映射(命中板块记 medium,命中 `lead_stock` 名称记 high),事件聚合口径简化为"同板块 ≥2 独立来源当日提及"(非语义级同一事件判定);极性用正负面关键词表判断;Tushare `cctv_news` 作为可选备份源已接入(20260704 实跑命中16条,未直接使用于事件抽取,仅计数);confidence 0.50(20260704 实跑值) |
| `TushareResearchClient` 扩展 | ✅ | `src/research/data_sources.py` 新增 `previous_trading_date`(R3 热榜排名 diff 用)、`cctv_news`(R4 可选备份源,失败静默返回空) |
| orchestrator 执行顺序调整 | ✅ | `write_unit_artifacts` 由"R3/R4/R7 skeleton → R1 → R2"改为"R1 → R3(依赖 R1 磁盘产物)→ R4(自包含)→ R7(仍 skeleton)→ R2(依赖 R1/R3/R4/R7)",原因见 `orchestrator.py`/`r3_sentiment.py` 模块 docstring |
| `_raw_inputs` 文件名解析 bug 顺手修复 | ✅ | 原 `name.replace(".json","").replace(".jsonl","")` 会把 `xxx.jsonl` 错误剥成 `xxx l`(先剥 `.json` 导致残留尾部 `l`),影响 R1-R4 所有单元的 `inputs` 字段可读性(R1/R2 此前就已受影响,未被发现);改为按长后缀优先剥离,一并修复 |
| `run_research_orchestrator.py --date 20260704` 重跑验证 | ✅ | `orchestrator_status: partial`(R1-R4 complete,R5-R7 skeleton);`confidence: {R1: 0.55, R2: 0.45, R3: 0.55, R4: 0.5, R5-R7: 0}` |
| 降级路径测试 | ✅ | 手工模拟:WeFlow 文件缺失→R3 confidence 0.55→0.50(符合预期);`TUSHARE_TOKEN` 清空→R3 confidence 降到 0.30(仅剩 KOL+WeFlow);R4 两个本地数据源文件都不存在→按预期抛 `RuntimeError` 被 orchestrator 捕获降级为 skeleton,均未导致进程崩溃 |
| R2 是否同步升级为消费 R3/R4 真实数据 | ⬜ 本批次不做 | 刻意保留的范围边界:R2 的 `score_dims.共振`/`score_dims.叙事` 目前仍留空,后续可接到 `R3_sentiment.json.resonance_top_themes` 和 `R4_news.json.sectors_catalyzed` |

**canghai-research R5/R6/R7 子进度**(2026-07-04 21:00 新增):

| 子任务 | 状态 | 备注 |
|---|:-:|---|
| R7 资金面:规则版 v1(非 LLM) | ✅ | `src/research/r7_capital_flow.py`;`batch: "premarket_full"`,`midday_increment: null`(留给 P2);北向资金(20日趋势,`moneyflow_hsgt`)、主力资金(概念板块净流入/流出前10,`moneyflow_ind_dc` 等)、游资(`hm_detail` 近30日活跃席位 + 3日协同信号)、ETF/期货(股指期货基差 `futures_close`+`index_close` 近似、ETF净流入用份额变化估算 `fund_share_change`)、跷跷板对(10日窗口5+5分段均值反转检测 + Pearson相关系数,自定义算法,spec未给出精确定义)五个子模块;20260704 实跑confidence 0.75,单次约35秒(多次Tushare调用+跷跷板相关系数计算,可接受) |
| R5 情报深挖:规则版 v1(非 LLM) | ✅ | `src/research/r5_stock_profiles.py`;对 R2 `main_lines[].leaders` 去重后的候选票逐票六维画像:催化背景(复用R4)/筹码结构(股东户数趋势+`hm_detail`近30日代理龙虎榜+近5日主力资金)/板块联动(R2得分+个股vs板块5日涨跌,ETF/期货联动因缺映射表未计算)/估值与财务(PE/PB历史分位+ROE+营收净利同比)/技术形态(MA排列+振幅+量比+20日累计涨跌,自算非四色四量)/研究覆盖(近30日研报+机构调研);`alpha_grade`/`risk_grade` 为规则版自定义评分公式(非spec强制标准);20260704 实跑 8只候选全部成功,confidence 0.70,单次约34秒 |
| R6 反证:规则版 v1(非 LLM) | ✅ | `src/research/r6_devil_advocate.py`;**红线1严格遵守:不发起任何新数据抓取/MCP调用,纯读磁盘 R1-R5+R7 JSON**;六类模式中模式一(数据源冲突,soft)/模式二(一致性检查,strong)/模式三(估值×主线临界,strong/soft)/模式六(霸榜出货,**唯一hard_reject**)有真实判断逻辑,模式四(历史类比)因v1阶段无历史事件库、模式五(昨日预期错误连锁)因E1观察池追踪尚未实现,均诚实降级为"本批次不可判断,数据源不足"(不编造);模式六采用**保守化设计**:distribution_alert命中 **且** R5筹码结构显示近5日主力资金净流出**同时成立**才真正hard_reject,仅命中前者时降级为strong_suggest(避免"全系统唯一硬否决权"仅凭单一软指标误杀龙头,与`docs/agents/R6-反证Agent.md`字面定义存在口径差异,已在模块docstring和最终回复中提请用户确认);红线3(不许否决到候选池<3只)已实现为`_apply_red_line_3_guard`;20260704 实跑发现3条反证(估值临界,均soft/strong,未触发hard_reject),confidence 0.80 |
| R6 hard_reject 机制验证 | ✅ | 20260704 真实数据未触发 hard_reject 场景(当日无 distribution_alert),故新增 `tests/test_r6_hard_reject.py`(7个用例,`pytest` 全绿):用构造数据验证"双重确认触发hard_reject"/"仅命中单一条件降级strong_suggest"/"资金数据缺失时降级"/"板块不命中时不触发"/"红线3保护性降级"/"红线3在候选池充足时不触发"/"`build()`端到端把`check_mode6_distribution_dump`结果正确汇总进顶层`hard_rejects[]`" |
| `data_sources.py` 扩展(R7/R5用) | ✅ | 新增 `northbound_moneyflow_range`/`concept_moneyflow_table`/`hm_detail_market`/`hm_detail_range`/`index_close`/`futures_close`/`fund_share_change`(R7)、`stk_holdernumber_trend`/`moneyflow_recent`/`daily_basic_range`/`fina_indicator_latest`/`daily_kline_recent`/`report_rc_recent`/`stk_surv_recent`(R5) |
| orchestrator 接入 R5/R6/R7 真实产出 | ✅ | `write_unit_artifacts` 执行顺序调整为 `R1→R3→R4→R7(真实)→R2→R5(真实)→R6(真实)`,与此前skeleton版本执行顺序一致,只是把"写占位"换成"调真实builder";`_build_real_unit` 的 builder 映射新增 R5/R6/R7 三项 |
| `research_summary.json.hard_rejects` 回填 bug 修复 | ✅ | 此前 `build_research_summary()` 硬编码 `hard_rejects: []`(skeleton阶段遗留),现改为 `_read_r6_hard_rejects()` 从落盘的R6产物按 `status=="complete"` 条件回填,20260704当日为空(符合真实情况,未触发) |
| `orchestrator_status` 判断逻辑 | ✅ 未改动(确认逻辑本身已正确) | `build_research_summary()` 早已是"7个单元全complete才`complete`,0个`complete`才`skeleton`,否则`partial`"的正确逻辑(非本次新写,核对后确认无需修改);20260704 R1-R7全部真实产出后首次实测跑出 `orchestrator_status: complete` |
| `run_research_orchestrator.py --date 20260704` 重跑验证 | ✅ | `orchestrator_status: complete`(7/7 complete);`confidence: {R1:0.55, R2:0.45, R3:0.55, R4:0.5, R5:0.70, R6:0.80, R7:0.75}`;`hard_rejects: []`(当日真实数据未触发) |
| `run_main_decision.py --date 20260704` 追加验证(D1侧) | ✅ 不崩溃,但"地雷"确认触发 | D1 读到 `orchestrator_status=complete` 后 `is_skeleton` 判 `False`,`mode` 从 `dry_run` 翻转为 `live`;但 `execution_pool`/`watch_pool` 仍为空池,`autonomous_trading_enabled=False`,`requires_user_confirmation=True`,**未产生任何真实交易信号,进程未崩溃**;此风险已在 R1/R2 阶段被预判到,本次是首次真实触发,已记入"已知风险"表新增第4项供用户决策 |

**canghai-plan 子进度**:

| 子任务 | 状态 | 备注 |
|---|:-:|---|
| D1 契约校验 `src/plan/main_decision.py` | ✅ | schema_version/trade_date/7 artifacts 存在性全部校验,不合规直接报错不写盘 |
| D1 skeleton plan 落盘 `data/plans/YYYYMMDD/watchlist_plan.json` | ✅ | 首跑 20260704,空池 `mode=dry_run` |
| 与旧 plan 对比 `legacy_comparison.json` | ✅ | 规模对比(execution/watch 计数 + mode),非字段级 diff |
| CLI + cron 入口 `scripts/plan/run_main_decision.{py,sh}` | ✅ | 待 hermes cron create(见 `cron/crontab.txt`,故意不提前挂,当前恒为空 plan) |
| 真实六步决策算法(docs/agents/D1-主决策Agent.md §3) | ❌ | 依赖 research v2 接入 LLM delegation 产出真实 R1-R7 |

**T1 能力注入留痕（2026-09-14 · 用户 sign-off 后落盘 · 新系统迁移清单）**：

> 用途：以下改动全部落在 `skills/`（external_dirs 加载，5 profile 自动继承）与 `src/`（T1 保留复用）。
> 迁移检查点：新系统若重构 src/，需按本清单手动迁移；skills/ 层无需迁移（external_dirs 指向项目目录即自动生效）。

| 能力 | 改动位置 | 内容 | 验证点（次日产物） |
|---|---|---|---|
| **买点后置硬闸门**（用户钦定纪律） | `skills/main_decision_v1/SKILL.md` Step 0.55 | ≤3 交易日内有宏观事件/行业级利空 → 高位票默认降级、买入后置 `post_event_only`、负面事件 cap×0.6 | `watchlist_plan.json` 的 `meta.event_premarket_gate` |
| **R4 利空×候选交叉** | `skills/main_decision_v1/SKILL.md` 关2.5 | `bearish_scope=行业级`命中板块 → 默认剔出 execution，否则需 sign-off | 候选票 `bearish_hit` 字段 |
| **消息面正反两面检查**（错题本 004 固化） | `skills/analyze_news_impact_v1/SKILL.md` Step2/3 | 同一主体正反面拆两条事件；负面强排；`bearish_scope`/`bearish_impact_sectors` 入 JSON 契约 | `R4_news.json` 出现 bearish 字段 |
| **复盘错题类别速查** | `skills/daily_review_v1/SKILL.md` Step7 | 新增 `R4消息面正偏` 类别 + 高位吃面归因流程 | E1 错题本自动关联 |
| **情绪周期时序守卫** | `src/research/compute_cycle_metrics.py` | 盘前当日数据未生成 → 回退 T-1 收盘口径（修复每天误判"低位试错"） | `emotion_cycle.json` 的 `metrics_date`/`intraday_fallback` |
| **全市场复盘管道**（用户钦定复盘看全市场） | `src/review/daily_review_prep.py` `build_market_panorama` | 涨停池/跌停池/近跌停大面/连板天梯/晋级率/全A涨跌分布/板块龙头对照 | `E1_data.json` 的 `market_panorama` |
| **规则感知涨跌停阈值**（金融监管知识库落地） | `src/review/daily_review_prep.py` `_breadth` | 每票按板块+ST 限幅判近跌停，非平铺 -9%；`special_period` 标记无限制期间 | 近跌停大面带 `limit_pct` 字段 |
| **竞价时点校正** | cron `d372940a8886`/`d5cd79c48615` | 竞价数据 09:27 才到 → fetch 09:27、P1 09:28 | 竞价快照 captured_at |
| **多平台人气股管道**（用户钦定"人气=潜力核心"） | `scripts/research/attention_pipeline.py` + fetch_all_sources 接入 + `skills/{R3,D1,E1}` | 东财人气/飙升榜 + 同花顺热股/概念 + persistent 90日持续人气 + surge 跳升 + 多平台合力；累积文件 `data/runtime/attention_accum.json`；D1 人气维度（attention_score/surge/cold）、E1 人气股复盘对照 | `attention_signals.json`（persistent/surge/cross_platform） |
| **provider 切换** | 5 profile config + 17 cron | coding-plan 周配额爆 → 全切 `volcengine-agent-plan`/flash-ga（备份 `.bak_20260913_221142`） | cron last_status=ok |

**备份**：4 个 profile config `config.yaml.bak_20260913_221142`（切换前）。

---

## T2 · Execute 层重构 —— ⬜ 未开始

依赖 T1 完成(尤其是 git 仓库化,否则不建议动 execute)。

| 动作 | 状态 |
|---|:-:|
| T2.1 `amygdala.py` 硬风控 + `watcher.py` 秒级 tick(与 intraday-once 并行) | ⬜ |
| T2.2 `market_snapshot.py` + `sector_stats.py` 分钟级 | ⬜ |
| T2.3 `market_scanner.py`(归 fetch)5 分钟异动 | ⬜ |

---

## T3 · Plan 层 P1/P2 + Review 层 E1/E2 补强 —— ⬜ 未开始

| 动作 | 状态 |
|---|:-:|
| P1 竞价校准上线(observe-only 起步) | ⬜ |
| P2 午盘修订上线 | ⬜ |
| E1 `daily_review_v1` 完整实现(双重复盘+missed_alpha+preferences 兑现率) | ⬜ |
| E2 `weekly_evolution_v1` 完整实现 | ⬜ |
| 判断节点选型 + 小范围试点 | ⬜ |

---

## T4 · 全量切换 + 旧系统下线 —— ⬜ 未开始

## T5 · 稳定期 + E2 首次修订生效 —— ⬜ 未开始

---

## 已知风险 / 待决策项(需要用户拍板)

| # | 问题 | 影响 | 建议 |
|---|---|---|---|
| 1 | ~~项目不是 git 仓库~~ → 2026-07-04 18:40 已 `git init` + 首次 commit(`42ac1ad`) | ~~T2 一旦出 bug 无法回滚~~ 已解除 | 后续每完成一个有意义的施工节点(如 T1 三段 artifact 链跑通)建议就地 commit 一次,不要攒到 T 阶段结束才提交,减小单次 diff 体积 |
| 2 | ~~`amendment_schema.md`、P1/P2 agent spec 仍缺~~ → 2026-07-04 19:00 已补齐(均为 skeleton) | 已解除 | T3 施工时需要按实盘 observe-only 结果细化 `amendment_schema.md` §4 的具体触发阈值,当前只是通用结构骨架 |
| 3 | ~~QMT REST(8788)未启动~~ → 2026-07-04 20:25 用户已在 Windows 端起服务,实测 `curl http://192.168.3.17:8788/v1/status`/`v1/holdings` 均正常返回真实持仓(`summary.cash`/`total_asset`/`market_value` 字段此前报过的 0.0 bug 已随券商修复一并验证正常);`.env` 已补 `AGENTQMT_QMT_API_URL`/`AGENTQMT_QMT_API_TOKEN`(复用 MCP token)/`AGENTQMT_QMT_API_PORT` | 已解除,T2 execute 层可以直接用 `requests` 调 REST 了 | **新发现的小问题**:同一时刻测试 MCP(8787)返回 `Connection failed...timed out`(`agentqmt_qmt` 状态 `down`),REST 和 MCP 用的都是 `session_id=123456`(文档建议 REST 应设 `QMT_SESSION_ID=123457` 避免冲突,但实测 REST 返回里 `session_id` 仍是 `123456`,说明 Windows 端目前可能没有真正区分两个 session,或者只跑了其中一个)。不阻塞 T2(T2 只用 REST),但如果之后还需要交互式 MCP 查询持仓(比如 Hermes/Cursor 里临时问一句"现在持仓多少"),可能会连不上,建议用户有空时在 Windows 端确认一下 MCP 服务是否还在跑、两个 session id 是否真的分开了 |
| 4 | ~~2026-07-04 21:00:R5/R6/R7 升级为真实产出后,`orchestrator_status` 首次跑出 `complete`,导致 `is_skeleton` 判 `False`,`mode` 翻转为 `live`~~ → 2026-07-05 14:25 已修复:`src/plan/main_decision.py` 新增独立常量 `D1_ALGORITHM_V1_IMPLEMENTED = False`,`is_skeleton = research_is_skeleton or not D1_ALGORITHM_V1_IMPLEMENTED`,与 `orchestrator_status` 解耦 | 已解除。重跑 `run_main_decision.py --date 20260704` 验证:`orchestrator_status=complete` 时 `mode` 仍正确保持 `dry_run` | 后续 D1 六步决策算法(§3)真正实现时,把 `D1_ALGORITHM_V1_IMPLEMENTED` 改成 `True` 即可让 `mode` 恢复跟随 research 完成度联动,不需要再改判断逻辑本身 |

---

## API vs MCP 选型建议(execute 层持仓/下单该走哪个)

**结论:两条腿走路,不是二选一** —— 这是 `mcp-api-usage.md` 里官方推荐的模式,也符合 CANONICAL "execute 全 Python" 的原则:

| 层 | 用哪个 | 原因 |
|---|---|---|
| **canghai-execute 的 Python daemon**(`watcher.py` / `amygdala.py` / 未来的秒级 tick 循环) | **REST API(8788)** | daemon 是纯 Python 高频轮询,MCP 协议(JSON-RPC + session handshake + SSE)对一个内部脚本来说是不必要的开销和复杂度;REST 就是 `requests.get/post` 三行代码,超时/重试/日志都更好控制,和"execute 层不受 LLM/协议层影响"的宪法原则(§1.2 路径 C)更契合 |
| **Hermes/Cursor 的 LLM 交互层**(research/plan/review 里偶尔要查一下持仓、iLink 提醒里带个仓位快照) | **MCP(8787)** | 这就是 MCP 存在的意义:Agent 自动选 tool,不用你写 HTTP client 代码 |

**当前的现实阻碍**:测试时 REST(8788)端口连接超时,说明 Windows 端目前**只起了 MCP 服务,没起 REST 服务**。要落地上面的建议,需要:
1. Windows 端额外跑一次 `.\scripts\start_api_qmt.ps1`,并设 `QMT_SESSION_ID=123457`(避免和 MCP 的 123456 冲突,同一账号两个连接会话不能共用 session id)
2. `.env` 里补 `AGENTQMT_QMT_API_TOKEN` / `AGENTQMT_QMT_API_PORT`(REST 专属,可以和 MCP token 相同)
3. T2 施工 `watcher.py`/`amygdala.py` 时直接用 `requests` 调 REST,不引入 MCP client 依赖

**这个决策不是本次紧急项**,T2 开工前(至少还有 1-2 周)落地即可,先记录进这份进度表。
